#!/usr/bin/env python3
"""Traveler Identity Immutable Kernel (TIIK)."""

IMMUTABLE_KERNEL = {
    "name": "TravelerIdentityImmutableKernel",
    "purpose": "Absolute core of identity that cannot change",
    "definition": "I_immutable = I(t) - delta(I(t)) — identity minus all change",
    "components": ["kernel_vector", "kernel_phase", "kernel_resonance",
                   "kernel_amplitude", "kernel_signature"],
    "purpose_desc": "Anchors final amplitude collapse, persists through zero-state",
    "output": ["vector", "phase", "resonance", "amplitude", "signature"]
}
